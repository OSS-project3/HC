현재 프론트엔드와 백엔드가 사용자 신청 완료 이후 관리자 페이지에서 검토·만세력 계산·작명·카드 제작으로 이어지는 전체 프로세스를 서로 동일하게 이해하고 있는지 코드와 문서를 직접 대조해서 점검해 주세요.

이번 작업의 목적은 구현이 아니라 프론트/백엔드 계약 및 상태 흐름 불일치 탐지입니다.
코드는 수정하지 말고 분석만 해주세요.

점검 범위는 반드시 사용자 신청 생성 → 관리자 신청 조회 → 결제/검토 → 출생지역/시간대 판정 → 만세력 계산/확정 → 작명 → 카드번호/디자인/미리보기/생성 → 제작/완료까지 전체 흐름으로 잡아 주세요.

특히 아래 항목을 빠짐없이 확인해 주세요.

상태 enum / 상태 전이 일치 여부
프론트와 백엔드에서 사용하는 ApplicationStatus, PaymentStatus, 만세력 관련 상태, 카드 생성 관련 상태 등이 같은지
동일한 enum 이름이라도 의미나 사용 시점이 다른 부분이 있는지
프론트가 기대하는 상태 전이 순서와 백엔드가 실제 허용하는 전이 순서가 같은지
특정 상태에서 프론트는 버튼/화면을 노출하지만 백엔드는 해당 API를 거절하는 경우가 있는지
반대로 백엔드는 가능한데 프론트에서 진입 경로가 없는 상태 전이가 있는지
사용자 신청 완료 후 관리자 페이지로 넘어오는 데이터 계약
사용자 신청 시 프론트가 보내는 DTO와 백엔드 Request DTO 비교
백엔드가 저장한 값 중 관리자 상세 조회 DTO에 빠지는 값이 있는지
관리자 화면이 필요로 하는 값인데 백엔드 응답에 없는 필드가 있는지
같은 의미인데 이름이 다른 필드가 있는지
nullable / required 조건이 서로 다른 필드가 있는지
개인/단체/학생증별로 계약이 다르게 해석되는 부분이 있는지

만세력 전체 흐름 대조

출생지역 검색
timezone/DST resolve
EXACT, AMBIGUOUS_LOCAL_TIME, NONEXISTENT_LOCAL_TIME
프론트 manseryeok 계산
timeAccuracy, uncertainPillars
관리자 확인
확정 POST
활성 결과 GET
NamingCard 표시

위 단계마다:

프론트가 보내는 값
백엔드가 기대하는 값
백엔드가 반환하는 값
프론트가 실제로 소비하는 값
을 대조해 주세요.

특히 다음을 확인해 주세요.

백엔드의 활성 ManseryeokResult가 실제 NamingCard의 Source of Truth로 연결되어 있는지
예전 computeMemberSaju, mockSaju, mockRecommendations 등의 로컬/mock 경로가 아직 운영 화면에서 남아 있는지
출생시간 미상일 때 hour pillar가 없는 경우 프론트 타입/렌더링이 안전한지
PARTIAL을 프론트 또는 백엔드 한쪽만 사용하고 있는지
calculationEngineVersion, inputHash, selectedOffset, utcInstant, timezoneId의 책임이 양쪽에서 동일한지
관리자 NamingCard / 작명 흐름
만세력 미확정 상태에서 프론트가 무엇을 보여주는지
만세력 확정 이후 사주/오행/추천 이름이 어떤 값을 Source of Truth로 쓰는지
작명 완료 조건과 백엔드 상태 제한이 일치하는지
프론트에서 작명 완료로 보이는데 백엔드는 아직 다른 상태인 경우가 있는지
applyNamingResult, assignMemberName 등 동일 목적의 여러 API가 서로 다른 상태 규칙을 갖는지
카드번호 / 디자인 / issueDate / 미리보기 / 실제 생성
디자인 목록은 active=true만 조회하는지
작명 완료 + 카드번호 + 활성 만세력 확정 조건이 프론트/백엔드 모두 동일한지
cardDesignId, cardIssueDate, issuerName, 카드번호 등의 저장 위치와 DTO가 일치하는지
Preview와 Generate가 같은 선행조건을 기대하는지
FRONT/BACK 미리보기 응답 계약이 현재 프론트와 백엔드에서 동일한지
실제 카드 생성 후 저장되는 cardFrontPath, cardBackPath, issueDate 등을 프론트가 정확히 반영하는지

프로세스 순서 자체에 대한 구조적 불일치
단순 필드명이 아니라, 프론트와 백엔드가 아예 서로 다른 업무 순서를 상정하는 부분을 찾아 주세요.

예:

프론트: 만세력 확정 전에 작명 가능
백엔드: 만세력 확정 후에만 작명 가능

또는

프론트: 디자인 선택 → issueDate 선택 → 미리보기
백엔드: Application에 디자인/issueDate가 이미 확정돼 있어야 Preview 허용

이런 식의 프로세스 모델 자체가 다른 부분을 별도로 찾아 주세요.

API는 있는데 프론트 미연동 / 화면은 있는데 백엔드 미연동
양방향 모두 찾으세요.
백엔드 API 존재 + 프론트 호출 없음
프론트 화면/버튼 존재 + 실제 API 호출 없음
프론트가 mock/local/sessionStorage만 사용
API 함수는 있는데 실제 컴포넌트 사용처 없음
백엔드 DTO 필드는 있는데 프론트 타입에 없음
문서와 실제 코드 불일치
docs/, docs/collab/, API 문서, TODO, 체크리스트 등 관련 문서와 실제 구현을 비교해서:
문서는 최신인데 코드가 옛날인 경우
코드는 최신인데 문서가 옛날인 경우
서로 다른 문서가 충돌하는 경우
를 구분해 주세요.

분석 방식은 반드시 실제 코드 검색 + 호출부 추적 + DTO/enum 정의 확인으로 해주세요. 이름만 보고 추측하지 말고, 실제로 어느 컴포넌트가 어느 api.ts 함수를 호출하고, 그 함수가 어떤 백엔드 endpoint와 DTO로 연결되는지 추적해 주세요.

결과는 아래 형식으로 정리해 주세요.

A. 전체 프로세스 맵

실제 현재 코드 기준으로 프론트→백엔드→프론트 흐름을 순서도로 정리

B. 완전히 일치하는 부분

상태/DTO/API/흐름이 이미 맞는 부분

C. 불일치 목록

각 항목마다:

단계
프론트 현재 동작
백엔드 현재 동작
문서상 기대 동작
실제 문제
심각도: BLOCKER / HIGH / MEDIUM / LOW
어느 쪽을 고치는 게 자연스러운지
D. API 미연동 목록
API 존재하지만 프론트 미사용
화면 존재하지만 API 미사용
mock/local fallback 사용 중
E. Enum / 상태전이 비교표

프론트 enum, 백엔드 enum, 허용 전이, UI 노출 조건을 한 표로

F. DTO 비교표

주요 단계별 Request/Response 필드를 프론트/백엔드 나란히 비교

G. 구조 자체가 다른 부분

단순 버그가 아니라 프론트와 백엔드가 서로 다른 프로세스를 상정하고 있는 부분만 별도 정리

H. 수정 우선순위
지금 바로 맞춰야 하는 계약 불일치
관리자 화면 연결 전 맞춰야 하는 것
후속 개선으로 미뤄도 되는 것

중요 제약

이번에는 절대 코드 수정하지 마세요.
TODO/문서도 수정하지 마세요.
테스트 코드도 작성하지 마세요.
분석 결과만 보고해 주세요.
이미 확정된 정책과 충돌하는 부분이 발견되면 임의로 한쪽에 맞추지 말고 반드시 “충돌”로 표시해 주세요.
특히 프론트 mock/local 계산이 실제 운영 데이터처럼 보이는 부분은 빠짐없이 찾아주세요.