package com.example.honorcitizen.common.enums;

// 학생증(STUDENT) 카드 앞·뒷면 텍스트 색상 선택지(docs/specs/application/checklist.md §6).
// 배경 템플릿에 이미 포함된 글자·사진·로고·직인·십이간지 이미지에는 적용되지 않는다.
public enum StudentTextColor {
    DARK_GRAY,
    WHITE
}
